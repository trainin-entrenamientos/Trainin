import { CorreccionPosturaComponent } from './correccion-postura.component'

describe('CorreccionPosturaComponent', () => {
  it('should mount', () => {
    cy.mount(CorreccionPosturaComponent)
  })
})